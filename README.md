# Wordforge
Wordforge is a program that aims to make revising for English more enjoyable.

## Home Directory
Wordforge requires its own directory to be setup in your home folder. Ensure that a wordforge folder is in your home directory, a subdirectory called "forge" is in the wordforge directory, and the HOME environment variable is set.